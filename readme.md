# Índices Moveleiro — SteelBenchmarker + Drivers (TE/BCB/FRED)

## Rodar localmente
```bash
python -m venv .venv
# Windows:
.venv\Scripts\activate
# Mac/Linux:
source .venv/bin/activate

pip install -r requirements.txt
streamlit run app.py
